@using ShoeStoreManagement.Core.Models
@using ShoeStoreManagement.Views.Shared.Components;
@using ShoeStoreManagement.Core.ViewModel;
@using ShoeStoreManagement.Core.Enums;

@model VoucherVM

@{
	var nProducts = Model.page;

	int countPage()
	{

		int numberPages = 0;
		int numberVouchers = 0;

		foreach (Voucher i in Model.vouchers)
		{
			numberVouchers++;
		}

		numberPages = numberVouchers / 10;

		if (numberVouchers % 10 != 0)
			numberPages++;

		return numberPages;
	}
}

<div class="row borderCustom">
	<div class="col-2 borderCustom1">
		<div class="row">
			<ii class="row-1 h6 mb-2 mt-4">Category</ii>

			<div class="tabCustom row-cols-6 w-100" style="max-height:260px; overflow-y:scroll; overflow-x:hidden">
				<a class="tabCustom" style="height:50px;" asp-action="Index" asp-route-filter="All" type="button">All</a>
				<a class="tabCustom" style="height:50px;" asp-action="Index" asp-route-filter="Using" type="button">Using</a>
				<a class="tabCustom" style="height:50px;" asp-action="Index" asp-route-filter="Expired" type="button">Expired</a>
				<a class="tabCustom" style="height:50px;" asp-action="Index" asp-route-filter="ComingSoon" type="button">ComingSoon</a>
			</div>
		</div>
	</div> @*used to col-3 vs col-10*@

	<div class="container col-10">
		<div class="row">
			<div class="col-md-12" id="hihi">
				<partial name="_ViewAll" model=@Model.vouchers/>
			</div>
		</div>
	</div>
</div>

<a onclick="showContent2('@Url.Action("Create","Voucher",null,Context.Request.Scheme)','Add new Voucher')" class="border-1 table-icon btn btn-primary icon-plus">
</a>